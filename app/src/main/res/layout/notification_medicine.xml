<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/startTimeField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:hint="Pradžios laikas (HH:mm)"
        android:inputType="time" />

    <EditText
        android:id="@+id/timesPerDayField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Kartai per dieną"
        android:inputType="number" />

    <EditText
        android:id="@+id/gapBetweenField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tarpas tarp išgerimo (minutės)"
        android:inputType="number" />

    <EditText
        android:id="@+id/intervalField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Dienų skaičius"
        android:inputType="number" />
</LinearLayout>
